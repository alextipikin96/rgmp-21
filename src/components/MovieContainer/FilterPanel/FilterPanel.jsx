import React from "react";
import "./FilterPanel.scss";

export default () => (
  <div className="FilterPanel">
    <span className="FilterPanel-sortby">SORT BY</span>
    <span className="FilterPanel-release">RELEASE DATE</span>
  </div>
);
